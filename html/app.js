// Declare app level module which depends on filters, and services
//var app = angular.module('dojoAdmin', ['dojoAdmin.filters', 'dojoAdmin.directives', 'ngWebsocket']);
var app = angular.module("dojoAdmin", ['ngWebsocket']);
